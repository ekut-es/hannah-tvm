# @package _group_

name: jetsontx2
target: "nvidia/jetson-tx2" 
target_host: "llvm -mtriple=aarch64-linux-gnu -device=arm_cpu -model=jetsontx2" 
opencl: True
cuda: True
hardware_params:
  max_shared_memory_per_block: 65536 

